# MongoDB connection string
# Option 1: Local MongoDB (requires MongoDB running locally)
MONGODB_URI=mongodb://127.0.0.1:27017/bytebattle

# Option 2: MongoDB Atlas (cloud) - replace with your connection string
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bytebattle?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mohamedrayen.elfidha@gmail.com
SMTP_PASS=pcru hbyh cllz pecs
SMTP_FROM=ByteBattle <noreply@bytebattle.com>

# WebAuthn / Face ID Configuration
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:5173
